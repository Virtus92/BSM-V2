# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=your-anon-key

# N8N Integration
# If using docker-compose, N8N_BASE_URL should be http://n8n:5678 inside the app container.
N8N_BASE_URL=http://n8n:5678
N8N_API_KEY=generated-at-setup
N8N_WEBHOOK_URL=http://n8n:5678/webhook
N8N_WEBHOOK_TEST_URL=http://n8n:5678/webhook-test

# Public N8N Webhook URL for @n8n/chat component
NEXT_PUBLIC_N8N_WEBHOOK_URL=https://n8n.dinel.at/webhook

# Landing Page AI Agent Configuration
NEXT_PUBLIC_LANDING_AGENT_WEBHOOK_ID=2CmJDOzuxIBZ2WP5
NEXT_PUBLIC_LANDING_AGENT_NAME=Business Assistant

# n8n backing Postgres (docker-compose)
N8N_DB_USER=n8n
N8N_DB_PASSWORD=changeme
N8N_DB_NAME=n8n
N8N_ENCRYPTION_KEY=generated-at-setup

# Hinweis: Chat Trigger erwarten standardmäßig den Pfad /chat und das Feld "chatInput" im Request-Body.

# Notifications (optional, no n8n required)
# In‑App notifications always work without any external config.
# Slack and Email are optional. Leave empty to disable external delivery.

# Slack incoming webhook URL to receive notifications (optional)
NOTIFICATIONS_SLACK_WEBHOOK_URL=

# Email notifications via Resend (optional)
# If RESEND_API_KEY is empty, email notifications are disabled.
# Supabase Auth emails (login, magic links, invites) are configured in Supabase Studio and do not use these vars.
RESEND_API_KEY=

# Optional. If omitted, a default fallback address is used (notifications@localhost)
NOTIFICATIONS_EMAIL_FROM=
